//

QUnit.test( "Chapter 1, Lesson 3 - Intro to Objects", function( assert ) {
  assert.equal(typeOfA, "string", "Got the type of variable `A`");
  assert.equal(typeOfB, "object", "Got the type of variable `B`");
  assert.equal(typeOfC, "string", "Got the type of variable `C`");
  assert.equal(typeOfD, "object", "Got the type of variable `D`");
  assert.equal(typeOfE, "object", "Got the type of variable `E`");
  assert.equal(typeOfF, "function", "Got the type of variable `F`");
});