/*

Write a function called getDaysOverdue which takes an ISO-formatted date string (e.g. "2016-04-18T13:38:14.855Z").

This function should return the number of days between the given date and the current date/time.

Since the library charges late fees for COMPLETE days overdue, the function should round down to the nearest day(e.g. if 4 days and 19 hours have passed, it should return 4)

If the date passed to the function is greater than the current date/time, your function should return `null` to indicate that the book is not overdue.

*/

/*
write function below these lines
*/
var getDaysOverdue = function(dateString){
    
};
